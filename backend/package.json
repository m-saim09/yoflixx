{
  "name": "backend",
  "version": "1.0.0",
  "description": "Yoflix Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node --check server.js && node --check app.js && node --check config/db.js && node --check config/env.js && node --check models/Admin.js && node --check models/Contact.js && node --check models/Lead.js && node --check models/PricingPlan.js && node --check models/WebsiteSettings.js",
    "seed": "node seed.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^6.2.0",
    "mongoose": "^8.9.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
